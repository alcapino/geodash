// src/environment.js

const environment = {
  mapapi: {
    key: 'string',
    lng: 'int',
    lat: 'int',
    zoom: 'int',
    options: {
      mapTypeControl: 'boolean',
      mapTypeId: 'const'
    }
  } 
};

export default environment;